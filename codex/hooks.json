{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Shell|UnifiedExec",
        "hooks": [
          {
            "type": "command",
            "command": "repo=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; exec /usr/bin/env python3 \"$repo/codex/hooks/require-semble-for-search.py\"",
            "timeout": 10,
            "statusMessage": "Checking search tool choice"
          }
        ]
      }
    ]
  }
}
